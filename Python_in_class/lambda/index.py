# lambada 

def square(x):
    sq = lambda x:
print(sq(5))
        
#lambda with osrted () - real ai use

students = [("Krihsna", 78),("subohd", 09), ("beta", 89)]
ranked = sorted(students, key = lambda s: s[1], reverse= True)
print(ranked)